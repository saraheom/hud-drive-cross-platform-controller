# Android app

This directory is reserved for the native Android HUD controller. The Android GitHub Actions workflow is already present and will become active automatically when `android/gradlew` exists.

Planned responsibilities:

- BLE scanning and Nordic UART connection
- Shared HUD packet encoder
- Initialization and RX event logging
- Navigation, brightness, dashboard, and diagnostic controls
