# iOS app

This directory is reserved for the Expo development-build iOS HUDWAY controller. The iOS GitHub Actions workflow becomes active when `ios/package.json` and `ios/eas.json` exist.

The repository secret `EXPO_TOKEN` will be required for EAS cloud builds.
